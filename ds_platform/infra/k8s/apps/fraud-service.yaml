apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: ds-platform
  labels:
    app: fraud-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: fraud-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: ds-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-service
  template:
    metadata:
      labels:
        app: fraud-service
    spec:
      containers:
      - name: fraud-service
        image: fraud-service:latest  # Replace with actual image
        ports:
        - containerPort: 8000
          name: http
        envFrom:
        - configMapRef:
            name: ds-platform-config
        - secretRef:
            name: ds-platform-secrets
        env:
        - name: SERVICE_NAME
          value: "fraud-service"
        - name: DOMAIN
          value: "fraud"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
      restartPolicy: Always
