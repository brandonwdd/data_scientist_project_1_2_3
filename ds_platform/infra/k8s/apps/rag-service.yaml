apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: ds-platform
  labels:
    app: rag-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: rag-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-service
  namespace: ds-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rag-service
  template:
    metadata:
      labels:
        app: rag-service
    spec:
      containers:
      - name: rag-service
        image: rag-service:latest  # Replace with actual image
        ports:
        - containerPort: 8000
          name: http
        envFrom:
        - configMapRef:
            name: ds-platform-config
        - secretRef:
            name: ds-platform-secrets
        env:
        - name: SERVICE_NAME
          value: "rag-service"
        - name: DOMAIN
          value: "rag"
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "4Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
      restartPolicy: Always
